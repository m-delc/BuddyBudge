import React from 'react'

// fake routes to prove auth
// fake routes to prove auth
// fake routes to prove auth
// fake routes to prove auth


const Fake1 = ({ user} ) => {
  console.log(user)
  return (
    <div>Fake1</div>
  )
}

export default Fake1